<h1>Choisissez une proposition de repas</h1>
<br>
<div class="d-flex justify-content-center mx-5">
  <div class="row">
    <div class="col-7">
      <div class="row justify-content-md-around justify-content-center flex-wrap">
        <% @offers.each do |offer| %>
          <%= link_to offer_path(offer), class: "col-10 col-md-8 col-lg-6 my-2 mx-md-0" do %>
            <%= render  "shared/card-offer", offer: offer %>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="col-5 col-md-5 pt-2 mx-md-0">
      <div id="map"
        style="width: 100%; height: 800px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
  </div>
</div>
