<h1><%= "Dashboard" %></h1>
<br>

<h2><%= "Bookings" %></h2>

<%# ---------------------TEST TABS SYSTEM------------------------- %>

<div class="container" id="container-dashboard">

  <ul class="nav nav-tabs border-0" id="myTab" role="tablist">
  <li class="nav-item col-4 m-0 p-0" role="presentation">
    <a class="pt-4 text-center nav-link border-0 active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Mes repas</a>
  </li>
  <li class="nav-item col-4 m-0 p-0" role="presentation">
    <a class="pt-4 text-center nav-link border-0" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Ma selection de repas</a>
  </li>
  <li class="nav-item col-4 m-0 p-0" role="presentation">
    <a class="pt-4 text-center nav-link border-0" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">Profil</a>
  </li>
</ul>

<div class="tab-content" id="myTabContent">

<%# ------------------1ere tabs----------------- %>
  <div class="container tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
  <%# < class=""><%= "Offers" %>
  <div class="bg-dashboard row justify-content-between">
  
  <%#= link_to "Add new offer", new_offer_path, class:"btn btn-secondary" %>

    <% @offers.each do |offer| %>
      <%= link_to offer_path(offer), class: "col-10 col-md-5 col-lg-3 m-2 mx-md-0 " do %>
      <%= render "shared/card-offer", offer: offer %>
    <% end %>
    
    </div>
 
  </div>

<%# ------------------2eme tabs----------------- %>
  <div class="container tab-pane fade show" id="home" role="tabpanel" aria-labelledby="home-tab">
  <%# < class=""><%= "Offers" %>
  <div class="bg-dashboard row justify-content-between">
  
  <%#= link_to "Add new offer", new_offer_path, class:"btn btn-secondary" %>
    
    <% @offers.each do |offer| %>
        <%= render "shared/card-offer", offer: offer %>
    <% end %>
    
    </div>
 
  </div>

  <%# ------------------3eme tabs----------------- %>
  <div class="container tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">3eme test</div>
</div>

</div>





